// eslint.config.js
module.exports = [
  {
    rules: {
      "semi": ["error", "always"],     // Enforce semicolons at the end of statements
      "no-unused-vars": "warn"         // Warn if variables are defined but never used
    }
  }
];